import React from "react";

const Amount = () => {
  return (
    <div
      style={{
        backgroundColor: "#039855",
        color: "#fff",
        width: "93px",
        height: "32px",
        borderRadius: "40px",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "16px",
      }}
    >
      ₦4,000
    </div>
  );
};

export default Amount;
